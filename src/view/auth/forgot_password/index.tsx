import RequestForgotPasswordForm from "./request/components/form";

export default function RequestForgotPasswordView() {
  return (
    <section className="min-h-screen flex items-center justify-center bg-white">
      <RequestForgotPasswordForm />
    </section>
  );
}
